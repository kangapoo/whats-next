{
  "name": "what-next-server",
  "version": "1.0.0",
  "description": "Netflix taste-matcher backend — real streaming availability (Movie of the Night) + ratings (OMDb), proxied so API keys stay server-side.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
